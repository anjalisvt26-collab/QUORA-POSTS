# QUORA-POSTS
This project is a REST API-based web application that uses CRUD operations to create, read, update, and delete data. EJS templates are used to connect the HTML and JavaScript with the backend. Each user action, such as viewing, creating, editing, or deleting a post, is handled through a separate route.
